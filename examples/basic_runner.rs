//====================================================================

use cabat::DefaultPlugins;
use cabat_runner::Runner;

//====================================================================

fn main() {
    Runner::run(|builder| {
        builder.add_plugin(DefaultPlugins);
    });
}

//====================================================================
